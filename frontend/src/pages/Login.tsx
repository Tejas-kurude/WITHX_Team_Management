import {FormEvent,useState} from 'react';import {useAuth} from '../context/AuthContext';import {messageOf} from '../services/api';import {Navigate} from 'react-router-dom';
export default function Login(){const {user,login}=useAuth();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [err,setErr]=useState('');const [busy,setBusy]=useState(false);if(user)return <Navigate to="/" replace/>;async function submit(e:FormEvent){e.preventDefault();setErr('');setBusy(true);try{await login(email,password)}catch(e){setErr(messageOf(e))}finally{setBusy(false)}}return <div className="min-h-screen bg-white md:grid md:grid-cols-2"><div className="flex items-center justify-center p-6"><div className="w-full max-w-md"><img src="/withx-logo.png" className="mb-8 h-20 object-contain"/><div className="mb-8"><h1 className="text-4xl font-extrabold text-navy">Welcome back</h1><p className="mt-2 muted">Sign in to the WITHX Management Platform.</p><div className="orange-line"/></div><form onSubmit={submit} className="space-y-4"><div><label className="label">Email</label><input className="input mt-1" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div><label className="label">Password</label><input className="input mt-1" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></div>{err&&<div className="rounded-lg bg-red-50 p-3 text-sm text-red-700">{err}</div>}<button className="btn btn-primary w-full" disabled={busy}>{busy?'Signing in...':'Sign In'}</button></form><p className="mt-6 text-xs muted">Accounts are created only by authorized administrators.</p></div></div><div className="hidden items-center justify-center bg-navy p-12 text-white md:flex"><div className="max-w-lg"><div className="text-sm font-bold uppercase tracking-[.2em] text-orange">WITHX Innovations</div><h2 className="mt-4 text-5xl font-extrabold leading-tight">One connected platform for company operations.</h2><p className="mt-5 text-lg text-white/70">Employees, attendance, tasks, daily work, leave, performance and analytics — together in real time.</p></div></div></div>}
