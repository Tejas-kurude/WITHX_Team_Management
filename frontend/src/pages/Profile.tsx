import {useEffect,useState} from 'react';
import {api,messageOf} from '../services/api';
import {PageTitle} from '../components/UI';
import {useAuth} from '../context/AuthContext';
export default function Profile(){const {user}=useAuth();const [p,setP]=useState<any>(null),[err,setErr]=useState('');useEffect(()=>{if(user?.employeeId)void api.get(`/employees/${user.employeeId}`).then(r=>setP(r.data)).catch(e=>setErr(messageOf(e)))},[user?.employeeId]);return <><PageTitle title="My Profile" subtitle="Your permanent WITHX employee/intern identity"/>{err&&<div className="rounded-xl bg-red-50 p-4 text-red-700">{err}</div>}{p&&<div className="card max-w-3xl p-6"><div className="mb-5 flex flex-wrap items-center justify-between gap-3"><div><div className="text-xs font-bold uppercase tracking-widest text-orange">{p.user_type}</div><h2 className="text-2xl font-extrabold">{p.first_name} {p.last_name}</h2></div><div className="rounded-xl bg-navy px-5 py-3 text-xl font-extrabold text-white">{p.employee_code}</div></div><div className="grid gap-4 sm:grid-cols-2"><div><div className="label">Email / Login ID</div><div>{p.email}</div></div><div><div className="label">Role</div><div>{p.role?.replace(/_/g,' ')}</div></div><div><div className="label">Department</div><div>{p.department_name||'—'}</div></div><div><div className="label">Team Lead</div><div>{p.team_lead_name||'—'}</div></div><div><div className="label">Job Title</div><div>{p.job_title||'—'}</div></div><div><div className="label">Joining Date</div><div>{new Date(p.joining_date).toLocaleDateString()}</div></div></div></div>}</>}
