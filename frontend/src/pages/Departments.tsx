import {FormEvent,useEffect,useState} from 'react';
import {api,messageOf} from '../services/api';
import {Empty,Modal,PageTitle} from '../components/UI';
import {useAuth} from '../context/AuthContext';

export default function Departments(){
  const {user}=useAuth();const isSuper=user?.role==='SUPER_ADMIN';const canCreate=['SUPER_ADMIN','ADMIN'].includes(user?.role||'');
  const [rows,setRows]=useState<any[]>([]),[emps,setEmps]=useState<any[]>([]),[detail,setDetail]=useState<any|null>(null),[show,setShow]=useState(false),[editing,setEditing]=useState<any|null>(null),[err,setErr]=useState(''),[pageErr,setPageErr]=useState(''),[q,setQ]=useState('');
  async function load(){try{setPageErr('');const r=await api.get('/departments',{params:{search:q}});setRows(r.data)}catch(e){setPageErr(messageOf(e))}}
  useEffect(()=>{void load();if(canCreate)void api.get('/employees').then(r=>setEmps(r.data)).catch(()=>{})},[]);
  async function openDetail(id:number){try{setDetail((await api.get(`/departments/${id}`)).data)}catch(e){alert(messageOf(e))}}
  async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();setErr('');try{const body=Object.fromEntries(new FormData(e.currentTarget).entries());if(editing)await api.put(`/departments/${editing.id}`,body);else await api.post('/departments',body);setShow(false);setEditing(null);await load()}catch(e){setErr(messageOf(e))}}
  async function remove(id:number){if(!confirm('Delete this department? It is blocked if employees/interns are still assigned.'))return;try{await api.delete(`/departments/${id}`);await load()}catch(e){alert(messageOf(e))}}
  return <><PageTitle title="Department Management" subtitle="Department members, heads and employee/intern counts" action={canCreate?<button className="btn btn-accent" onClick={()=>{setEditing(null);setShow(true)}}>+ Department</button>:undefined}/><div className="card mb-5 flex gap-2 p-4"><input className="input" placeholder="Search department" value={q} onChange={e=>setQ(e.target.value)}/><button className="btn btn-primary" onClick={()=>void load()}>Search</button></div>{pageErr&&<div className="mb-4 rounded-xl bg-red-50 p-4 text-sm text-red-700">{pageErr}</div>}{rows.length?<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{rows.map(r=><div className="card p-5" key={r.id}><div className="flex items-start justify-between gap-3"><div><h3 className="text-lg font-extrabold">{r.name}</h3><div className="orange-line"/></div>{isSuper&&<div className="flex gap-2"><button className="btn !px-3 !py-1.5" onClick={()=>{setEditing(r);setShow(true)}}>Edit</button><button className="btn !px-3 !py-1.5 text-red-600" onClick={()=>remove(r.id)}>Delete</button></div>}</div><p className="mt-4 text-sm muted">{r.description||'No description added.'}</p><div className="mt-4 grid grid-cols-3 gap-2 text-center"><div className="rounded-lg bg-slate-50 p-2"><b>{r.member_count}</b><div className="text-xs muted">Total</div></div><div className="rounded-lg bg-slate-50 p-2"><b>{r.employee_count}</b><div className="text-xs muted">Employees</div></div><div className="rounded-lg bg-slate-50 p-2"><b>{r.intern_count}</b><div className="text-xs muted">Interns</div></div></div><div className="mt-4 text-sm"><span className="muted">Head/Lead:</span> {r.head_name||'Not assigned'}</div><button className="btn mt-4 w-full" onClick={()=>void openDetail(r.id)}>View Department Details</button></div>)}</div>:!pageErr&&<Empty/>}{show&&<Modal title={editing?'Edit Department':'Create Department'} onClose={()=>{setShow(false);setEditing(null)}}><form className="space-y-4" onSubmit={save}><input className="input" name="name" placeholder="Department name" defaultValue={editing?.name||''} required/><textarea className="input min-h-28" name="description" placeholder="Description" defaultValue={editing?.description||''}/><select className="input" name="headEmployeeId" defaultValue={editing?.head_employee_id||''}><option value="">No Department Head</option>{emps.filter(e=>e.role==='TEAM_LEAD'||e.role==='ADMIN'||e.role==='SUPER_ADMIN').map(e=><option key={e.id} value={e.id}>{e.employee_code} — {e.first_name} {e.last_name}</option>)}</select>{err&&<div className="text-red-600">{err}</div>}<button className="btn btn-primary">{editing?'Save Changes':'Save Department'}</button></form></Modal>}{detail&&<Modal title={`${detail.name} Department`} onClose={()=>setDetail(null)}><div className="mb-4 text-sm muted">Head/Lead: {detail.head_name||'Not assigned'}</div>{detail.members?.length?<div className="table-wrap"><table className="table"><thead><tr><th>ID</th><th>Name</th><th>Type</th><th>Role</th><th>Team Lead</th></tr></thead><tbody>{detail.members.map((m:any)=><tr key={m.id}><td><b>{m.employee_code}</b></td><td>{m.first_name} {m.last_name}</td><td>{m.user_type}</td><td>{m.role}</td><td>{m.team_lead_name||'—'}</td></tr>)}</tbody></table></div>:<Empty/>}</Modal>}</>
}
