import {FormEvent,useEffect,useState} from 'react';
import {api,messageOf} from '../services/api';
import {Empty,Modal,PageTitle} from '../components/UI';
import {useAuth} from '../context/AuthContext';

export default function Employees(){
  const {user}=useAuth();const isSuper=user?.role==='SUPER_ADMIN';const canCreate=user?.role==='SUPER_ADMIN'||user?.role==='ADMIN';
  const [rows,setRows]=useState<any[]>([]),[deps,setDeps]=useState<any[]>([]),[show,setShow]=useState(false),[editing,setEditing]=useState<any|null>(null),[err,setErr]=useState(''),[pageErr,setPageErr]=useState(''),[q,setQ]=useState(''),[department,setDepartment]=useState(''),[userType,setUserType]=useState('');
  async function load(){try{setPageErr('');const r=await api.get('/employees',{params:{search:q,department,userType}});setRows(r.data)}catch(e){setPageErr(messageOf(e))}}
  useEffect(()=>{void load();void api.get('/departments').then(r=>setDeps(r.data)).catch(e=>setPageErr(messageOf(e)))},[]);
  async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();setErr('');const body=Object.fromEntries(new FormData(e.currentTarget).entries());try{if(editing)await api.put(`/employees/${editing.id}`,body);else await api.post('/employees',body);setShow(false);setEditing(null);await load()}catch(e){setErr(messageOf(e))}}
  async function remove(id:number){if(!confirm('Delete this user and related records? Their INT/EMP ID will NOT be reused.'))return;try{await api.delete(`/employees/${id}`);await load()}catch(e){alert(messageOf(e))}}
  return <><PageTitle title={user?.role==='TEAM_LEAD'?'My Team':'Employee & Intern Management'} subtitle={user?.role==='TEAM_LEAD'?'Only employees/interns assigned to you are shown':'System-generated INT/EMP IDs, department and team assignment'} action={canCreate?<button className="btn btn-accent" onClick={()=>{setEditing(null);setShow(true)}}>+ Add User</button>:undefined}/><div className="card mb-5 grid gap-3 p-4 md:grid-cols-4"><input className="input md:col-span-2" placeholder="Search ID, name or email" value={q} onChange={e=>setQ(e.target.value)}/><select className="input" value={department} onChange={e=>setDepartment(e.target.value)}><option value="">All departments</option>{deps.map(d=><option key={d.id} value={d.id}>{d.name}</option>)}</select><select className="input" value={userType} onChange={e=>setUserType(e.target.value)}><option value="">Employees + Interns</option><option value="EMPLOYEE">Employees</option><option value="INTERN">Interns</option></select><button className="btn btn-primary md:col-span-4" onClick={()=>void load()}>Apply Search / Filters</button></div>{pageErr&&<div className="mb-4 rounded-xl bg-red-50 p-4 text-sm text-red-700">{pageErr}</div>}{rows.length?<div className="table-wrap"><table className="table"><thead><tr><th>ID</th><th>User</th><th>Type</th><th>Department</th><th>Team Lead</th><th>Role</th><th>Status</th>{isSuper&&<th>Manage</th>}</tr></thead><tbody>{rows.map(r=><tr key={r.id}><td><b>{r.employee_code}</b></td><td><b>{r.first_name} {r.last_name}</b><div className="text-xs muted">{r.email}</div></td><td><span className="badge">{r.user_type}</span></td><td>{r.department_name||'—'}</td><td>{r.team_lead_name||'—'}</td><td>{r.role?.replace(/_/g,' ')||'—'}</td><td><span className="badge">{r.status}</span></td>{isSuper&&<td><div className="flex gap-2"><button className="btn !px-3 !py-1.5" onClick={()=>{setEditing(r);setShow(true)}}>Edit</button><button className="btn !px-3 !py-1.5 text-red-600" disabled={r.id===user?.employeeId} onClick={()=>remove(r.id)}>Delete</button></div></td>}</tr>)}</tbody></table></div>:!pageErr&&<Empty/>}{show&&<Modal title={editing?'Edit User':'Create Employee / Intern Account'} onClose={()=>{setShow(false);setEditing(null)}}><form className="grid gap-4 sm:grid-cols-2" onSubmit={save}>{!editing&&<div className="sm:col-span-2 rounded-lg bg-slate-50 p-3 text-sm muted">The ID is generated automatically and never reused: INT001... for interns, EMP001... for employees.</div>}<div><label className="label">First Name</label><input className="input mt-1" name="firstName" defaultValue={editing?.first_name||''} required/></div><div><label className="label">Last Name</label><input className="input mt-1" name="lastName" defaultValue={editing?.last_name||''} required/></div>{!editing&&<><div><label className="label">User Type</label><select className="input mt-1" name="userType"><option value="EMPLOYEE">Employee</option><option value="INTERN">Intern</option></select></div><div><label className="label">Email / Login ID</label><input className="input mt-1" name="email" type="email" required/></div><div><label className="label">Temporary Password</label><input className="input mt-1" name="password" type="password" minLength={8} required/></div><div><label className="label">Role</label><select className="input mt-1" name="role"><option>EMPLOYEE</option><option>TEAM_LEAD</option>{isSuper&&<><option>ADMIN</option><option>SUPER_ADMIN</option></>}</select></div></>}<div><label className="label">Phone</label><input className="input mt-1" name="phone" defaultValue={editing?.phone||''}/></div><div><label className="label">Job Title</label><input className="input mt-1" name="jobTitle" defaultValue={editing?.job_title||''}/></div><div><label className="label">Department</label><select className="input mt-1" name="departmentId" defaultValue={editing?.department_id||''}><option value="">Select department</option>{deps.map(d=><option key={d.id} value={d.id}>{d.name}</option>)}</select></div><div><label className="label">Team Lead</label><select className="input mt-1" name="teamLeadId" defaultValue={editing?.team_lead_id||''}><option value="">None / not assigned</option>{rows.filter(x=>x.role==='TEAM_LEAD').map(x=><option key={x.id} value={x.id}>{x.first_name} {x.last_name}</option>)}</select></div>{editing?<div><label className="label">Status</label><select className="input mt-1" name="status" defaultValue={editing.status}><option>ACTIVE</option><option>INACTIVE</option></select></div>:<div><label className="label">Joining Date</label><input className="input mt-1" type="date" name="joiningDate"/></div>}{err&&<div className="sm:col-span-2 rounded-lg bg-red-50 p-3 text-sm text-red-700">{err}</div>}<div className="sm:col-span-2 flex justify-end gap-2"><button type="button" className="btn" onClick={()=>{setShow(false);setEditing(null)}}>Cancel</button><button className="btn btn-primary">{editing?'Save Changes':'Create Account'}</button></div></form></Modal>}</>
}
